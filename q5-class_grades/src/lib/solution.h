#ifndef TEMPLATE_SOLUTION_H
#define TEMPLATE_SOLUTION_H

#include <string>
#include <vector>
#include <map>

struct Grades
{
  int math;
  int computers;
  int physics;
  Grades() : math(0), computers(0), physics(0) {}
  Grades(int x, int y, int z) : math(x), computers(y), physics(z) {}
}
class AcademicRecord
{
  private:
    Grades* grades;
  public:
    // constructors
    AcademicRecord
    

}

#endif